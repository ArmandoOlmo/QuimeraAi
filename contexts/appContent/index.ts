/**
 * AppContent Context Exports
 * Contenido global de la App Quimera (artículos, navegación, config del landing)
 */

export { 
    AppContentProvider, 
    useAppContent, 
    useSafeAppContent 
} from './AppContentContext';

export type { AppContentContextType } from '../../types/appContent';






